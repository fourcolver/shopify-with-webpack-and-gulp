/**
 * config
 * @type {Object}
 */
const config = {

	// classes
	cls: {
		active: 'is-active',
		content_active: 'content-is-active',
		loading: 'is-loading',
	},

	// ids
	id: {
		content: 'acContent',
	},

	// selectors
	s: {
		submit_btn: '.js-submit-btn',
	},

	// view model
	vm: {
		dashboard: '#acDashboard',
		msg: '#acMsg',
	},
};

export default config;